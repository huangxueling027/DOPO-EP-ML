# Information-source ablation results

This lightweight repository release retains the verified aggregate ablation tables used by the manuscript. Fold-level model binaries are intentionally omitted because they are reproducible from the frozen code/data workflow.

To regenerate the full ablation output, run the corresponding FINAL robustness workflow from the repository root.
